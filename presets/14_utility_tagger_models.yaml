# (14) utility tagger models list
name: utility_tagger_models
type: utility
description: "Get list of available Tagger models"

settings:
  action: "tagger_models"
  endpoint: "/tagger/v1/interrogators"
  method: "GET"